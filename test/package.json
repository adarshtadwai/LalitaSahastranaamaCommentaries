{
  "name": "lalita-sahasranama-tests",
  "version": "1.0.0",
  "description": "Tests for Lalita Sahasranama website",
  "scripts": {
    "test": "jest --config=jest.config.js",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "jest": "^29.7.0",
    "jest-playwright-preset": "^4.0.0",
    "playwright": "^1.40.0"
  }
}
